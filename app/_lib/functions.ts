export const newLineHardCoder = (name: string) => {
  if (name === 'Van Gogh self-portrait') return 'Van Gogh\nself-portrait';
  else if (name === 'Starry Night') return 'Starry\nNight';
  return name;
};
